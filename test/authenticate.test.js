
import Server from './../lib/oauth2-mini';

describe('Server', () => {
  describe('authenticate()', (done) => {
    let server = new Server();
    it(`it should be a test here ;)`, (done) => {
      //TODO: write tests!
      done();
    });
  });
});
